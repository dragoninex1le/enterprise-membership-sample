# @estyn scope resolves from the Porth CodeArtifact npm repository (porth-npm).
# Everything else stays on the public registry. The auth token is injected at
# install time via the CODEARTIFACT_AUTH_TOKEN env var (set by CI/deploy after
# `aws codeartifact get-authorization-token`; for local installs, export it yourself).
@estyn:registry=https://porth-084847995635.d.codeartifact.us-east-1.amazonaws.com/npm/porth-npm/
//porth-084847995635.d.codeartifact.us-east-1.amazonaws.com/npm/porth-npm/:_authToken=${CODEARTIFACT_AUTH_TOKEN}
