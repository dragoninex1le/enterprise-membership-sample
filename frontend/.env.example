# API base URL — used to fetch tenant config and all API calls
VITE_API_BASE_URL=https://<your-api-id>.execute-api.us-east-1.amazonaws.com/prod

# Local development only — set to your tenant ID (e.g. acme-dev)
# In production the tenant is resolved from the subdomain automatically
VITE_DEV_TENANT_ID=
